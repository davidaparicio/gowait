apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# kubectl apply -k deploy/k8s
# Edit configmap.yaml (backend URL, capacity) and secret.yaml (secrets!)
# before applying anywhere real.
resources:
  - configmap.yaml
  - secret.yaml
  - valkey.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
